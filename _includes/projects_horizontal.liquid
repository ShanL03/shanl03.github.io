<div class="col">
  <a href="{% if project.redirect %}{{ project.redirect }}{% else %}{{ project.url | relative_url }}{% endif %}">
    <div class="card h-100 hoverable project-overlay-card">
      {% if project.img %}
        <img src="{{ project.img | relative_url }}" alt="project thumbnail" class="project-bg-img">
        <div class="project-overlay-gradient"></div>
      {% endif %}
      <div class="card-body project-overlay-body">
        <h2 class="card-title">{{ project.title }} <i class="fa-solid fa-arrow-up-right-from-square project-arrow" style="font-size: 0.55em;"></i></h2>
        <p class="card-text">{{ project.description }}</p>
        <div class="row ml-1 mr-1 p-0">
          {% if project.github %}
            <div class="github-icon">
              <div class="icon" data-toggle="tooltip" title="Code Repository">
                <a href="{{ project.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
              </div>
              {% if project.github_stars %}
                <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                  <i class="fa-solid fa-star"></i>
                  <span id="{{ project.github_stars }}-stars"></span>
                </span>
              {% endif %}
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  </a>
</div>
